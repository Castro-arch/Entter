# Base URL of the Entter backend API. Exposed to the browser (NEXT_PUBLIC_),
# since the organizer dashboard is a client-side SPA that talks to the API
# directly and authenticates via the httpOnly session cookie.
NEXT_PUBLIC_API_URL="http://localhost:3000"
